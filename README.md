# 3D Animated Background Test Site

A single-page test site that uses `get.jfif` as a full-screen animated background with CSS 3D transforms, floating clipped image layers, and cursor/device-orientation parallax.

## Run locally

```bash
python3 -m http.server 8000
```

Then open <http://localhost:8000>.
